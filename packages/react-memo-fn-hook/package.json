{
  "name": "@hookify/react-memo-fn-hook",
  "version": "0.1.0",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "license": "Apache-2.0",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "yarn clean && ../../node_modules/.bin/rollup -c rollup.config.js --bundleConfigAsCjs",
    "prepublish": "yarn build"
  },
  "dependencies": {
    "shallowequal": "^1.1.0"
  },
  "devDependencies": {
    "@types/shallowequal": "^1.1.1"
  },
  "peerDependencies": {
    "react": "^16.8.0"
  }
}
